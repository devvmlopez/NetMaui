<?xml version="1.0" encoding="utf-8" ?>
<mopups:PopupPage 
             BackgroundColor="Transparent"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InntecMobileNetMaui.Views.CustomView.Card.CardNipPage"
             xmlns:mopups="clr-namespace:Mopups.Pages;assembly=Mopups"
             xmlns:mopupsanim="clr-namespace:Mopups.Animations;assembly=Mopups"
             xmlns:controls="clr-namespace:InntecMobileNetMaui.RendererControls"
             Title="{Binding Title}">
    <mopups:PopupPage.Animation>
        <mopupsanim:ScaleAnimation
          DurationIn="200"
          DurationOut="200"
          EasingIn="CubicOut"
          EasingOut="CubicIn" 
          HasBackgroundAnimation="True"
          PositionIn="Bottom"
          PositionOut="Bottom"
          ScaleIn="1"
          ScaleOut="1" />
    </mopups:PopupPage.Animation>
    <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <Frame
            HorizontalOptions="FillAndExpand"
            AbsoluteLayout.LayoutBounds="0,0,1,1" 
            AbsoluteLayout.LayoutFlags="All"  
            VerticalOptions="EndAndExpand"
            BackgroundColor="{DynamicResource backgroundminicolor}"
            CornerRadius="10">
            <StackLayout Margin="0,20,0,0">
                <Label Text="NIP" 
                          HorizontalOptions="Center" />
                <Entry x:Name="Txt_Nip"
                          Text="{Binding CardNipModel.Nip}"
                          HorizontalOptions="Center"
                          MaxLength="4"
                          Placeholder="0000"
                          IsPassword="True"
                          Keyboard="Numeric" >
                </Entry>
                <Label Text="Confirmar NIP" 
                         HorizontalOptions="Center" />
                <Entry x:Name="Txt_confirmar_Nip"
                         Text="{Binding NipConfirm}"
                         HorizontalOptions="Center"
                         MaxLength="4"
                         Placeholder="0000"
                         IsPassword="True"
                         Keyboard="Numeric" >
                </Entry>
                <StackLayout 
                    Orientation="Horizontal"
                    Margin="0,10,0,0"
                    HorizontalOptions="FillAndExpand">
                    <Button 
                        x:Name="Btn_ChangeNip"
                        x:FieldModifier="Public"
                        Text="Cambiar NIP"
                        TextColor="White"
                        HorizontalOptions="FillAndExpand"
                        Margin="0,0,-10,0"
                        BackgroundColor="#3487CB"/>
                    <Button 
                        x:Name="Btn_Cancel"
                        Text="Cancelar"
                        Pressed="Btn_Cancel_Clicked"
                        TextColor="White"
                        HorizontalOptions="FillAndExpand"
                        BackgroundColor="#f22b2b"/>
                </StackLayout>
            </StackLayout>

        </Frame>
        <AbsoluteLayout 
            HorizontalOptions="FillAndExpand"
            VerticalOptions="FillAndExpand"
            AbsoluteLayout.LayoutBounds="0,0,1,1" 
            AbsoluteLayout.LayoutFlags="All" 
            BackgroundColor="#00000000"
            IsVisible="{Binding IsBusy}">
            <Image 
               Source="loading.gif"
               HeightRequest="70"
               WidthRequest="70"
               IsAnimationPlaying="{Binding IsBusy}"
               IsVisible="{Binding IsBusy}"
               AbsoluteLayout.LayoutBounds="0,0,1,1"
               AbsoluteLayout.LayoutFlags="All"/>

        </AbsoluteLayout>
    </AbsoluteLayout>
</mopups:PopupPage>